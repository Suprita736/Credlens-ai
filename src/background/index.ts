// Background service worker for CredLens AI
chrome.runtime.onInstalled.addListener(() => {
  console.log('CredLens AI Extension installed.');
});

// Future: Handle cross-tab messaging or persistent state
export {};
